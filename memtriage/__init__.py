"""hermes-memory-triage plugin package.

Pure-stdlib core shared by the plugin surface (plugin/__init__.py), the
slash command, and the agent tool.  Cross-platform: no shell, no OS-specific
calls; paths built with pathlib.
"""

__version__ = "0.1.0"
